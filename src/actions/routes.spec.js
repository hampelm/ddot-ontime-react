import * as routes from './routes'


describe('Route actions', () => {
  describe('shouldFetchRoutes', () => {
    xit('should fetch if isFetching is false', () => {


    })
  })

  describe('receiveRoutes', () => {
    xit('process routes correctly', () => {

    })

    xit('process routes correctly', () => {

    })
  })
})
